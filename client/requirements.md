## Packages
framer-motion | Complex animations for card flips and page transitions
lucide-react | Icons for UI elements (already in base, but noting usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'Inter'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
  mono: ["'JetBrains Mono'", "monospace"],
}
